- name : install and run nginx
  hosts: web
  tasks:
  - name: install nginx
    ansible.builtin.package:
    name : nginx
    state : present
- name: enable nginx
  ansible.builtin.service:
  name : nginx
  state : enable
- name: install nginx
  ansible.builtin.service:
  name : nginx
  state : started